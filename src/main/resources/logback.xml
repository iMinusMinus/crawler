<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%gray(%d{yyyy-MM-dd HH:mm:ss.SSS}) %highlight(%5p) %gray(---) %gray([%15.15t]) %cyan(%-40.40logger{39}) %gray(:) %m%n%ex</pattern>
        </encoder>
    </appender>

    <logger name="robot.crawler" level="debug" />
    <root level="info">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>